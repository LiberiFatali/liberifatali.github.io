---
const { sideBarActiveItemID } = Astro.props;
const activeClass = "bg-base-300"; // For primary color replace with `active` class
---

<ul class="menu grow shrink menu-md overflow-y-auto">
    <li><a class="py-3 text-base" id="home" href="/" data-astro-prefetch>Home</a></li>
    <li><a class="py-3 text-base" id="projects" href="/projects" data-astro-prefetch>Projects</a></li>
    <li><a class="py-3 text-base" id="cv" href="/cv" data-astro-prefetch>Profile</a></li>
    <li>
        <a
            id="contact"
            class="py-3 text-base"
            href="#">Contact</a
        >
    </li>
</ul>

<script is:inline define:vars={{ sideBarActiveItemID: sideBarActiveItemID, activeClass: activeClass }}>
const activeItemElem = document.getElementById(sideBarActiveItemID);
activeItemElem && activeItemElem.classList.add(activeClass);

const contactLink = document.getElementById("contact");
if (contactLink) {
    const email = "moc dot liamg at anvsselc"
        .split("")
        .reverse()
        .join("")
        .replace(/ ta /g, "@")
        .replace(/ tod /g, ".");
    contactLink.href = "mailto:" + email;
}
</script>